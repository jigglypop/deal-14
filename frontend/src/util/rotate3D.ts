import { $ } from "./select"

const rotate3D = () => {
    // $("body").on('mousemove', function (e: any) {
    //     let Y = (window.innerHeight / 2 - e.pageY) / 20
    //     let X = (window.innerWidth / 2 - e.pageX) / 20
    //     $("#root").css("transform", `rotateY(${X}deg) rotateX(${Y}deg)`)
    //     $(`.HomeOuter`).css("transform", "translateZ(300px)")
    // })
    // $("body").on('mouseenter', function (e: any) {
    //     $("#root").css("transition", "translateZ(0)")
    // })
    // $("body").on('mouseleave', function (e: any) {
    //     $("#root").css("transform", `rotateY(0deg) rotateX(0deg)`)
    //     // $(`.HomeOuter`).css("transform", "translateZ(0)")
    // })
}

export default rotate3D